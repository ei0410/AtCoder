#include <bits/stdc++.h>

int main(int argc, char *argv[])
{
    int X;
    std::cin >> X;
    if ((X == 3) || (X == 5) || (X == 7)) {
        std::cout << "YES" << std::endl;
    } else {
        std::cout << "NO"  << std::endl;
    }
    return 0;
}